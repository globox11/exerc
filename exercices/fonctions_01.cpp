#include "fonctions_01.hpp"

/*
 * @name : afficher_nombre_entier
 * @description : Fonction permettant d'afficher le nombre passé en argument.
 * @param : nb_param : le nombre entier à afficher.
 * @return : void.
 *
 * @astuce : il faut utiliser la fonction printf contenue dans la bibliotheque standard.
 * Cette fonction est de la forme printf("format d'affichage avec %c, %d, %f ... + eventuellement caractère de fin de ligne \n", ... args_a_afficher).
 * Pour plus de détail sur cette fonction printf, lire la doc en ligne.
 */
//todo : mettre l'implémentation de la fonction ici.

/*
 * @name : afficher_parametre
 * @description : Fonction permettant d'afficher le tableau de caractère passé en argument.
 * @param : index : la position du paramètre dans le tableau de tableau.
 * @param : str_to_print : le tableau de caractères à afficher.
 * @return : void.
 *
 * @astuce : Il faut se rappeler qu'un tableau peut être représenté par un pointeur sur le premier élément de ce tableau et
 * que la fonction printf a une option de FORMATAGE %s. Cette fonction devra faire un affichage de la forme :
 *
 *           le paramètre à l'index N a pour valeur : VAL
 *
 * Ou N et VAL seront remplacés par les bonne valeurs.
 *
 */
//todo : mettre l'implémentation de la fonction ici.

/*
 * @name : add_integer
 * @description : Fonction permettant d'ajouter deux entiers.
 * @param : inte_01 : le premier nombre.
 * @param : inte_02 : le deuxieme nombre.
 * @return : un integer representant le resultat de l'addition.
 *
 * @astuce : il faut utiliser la fonction convert_string_to_int en amont de celle ci pour obtenir les deux paramètres à additionner.
 */
//todo : mettre l'implémentation de la fonction ici.

/*
 * @name : convert_string_to_int
 * @description : Fonction permettant de convertir un string en entier.
 * @param : str_to_convert : le tableau de caractère à convertir.
 * @return : un integer qui est le résultat de la conversion.
 *
 * @astuce : des fonctions dans la library standard existe pour effectuer cette conversion.
 */
//todo : mettre l'implémentation de la fonction ici.
